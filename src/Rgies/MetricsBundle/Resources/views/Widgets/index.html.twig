{% extends "MetricsBundle::layout.html.twig" %}

{% block content -%}
<div class="container">
    <div class="page-header">
        <h1>Widgets list</h1>
    </div>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Type</th>
                <th>Enabled</th>
                <th>Min</th>
                <th>Max</th>
                <th>Param</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('widgets_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.enabled }}</td>
                <td>{{ entity.min }}</td>
                <td>{{ entity.max }}</td>
                <td>{{ entity.param }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('widgets_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('widgets_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>    <div>
        <button type="button" class="btn btn-primary" onclick="location.href='{{ path('widgets_new') }}'">
            Create a new entry
        </button>
    </div></div>
{% endblock %}
